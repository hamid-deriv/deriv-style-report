const id_selectors_issues = `
- There isn't any usage for **platform_name** id in the app.
- **dt_set_account_currency_modal** id is used for styling the inner DOM which is not proper way of defining style. Also, the name convention of this id is for **data-testid**s, not for ids.
`;

export { id_selectors_issues };
